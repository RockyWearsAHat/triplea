# Triple A Muse - Netlify Configuration
# Deploy as: muse.<your-site>.netlify.app

[build]
  base = "TripleAMuse"
  publish = "dist"
  command = "cd .. && npm install && npm run build --prefix TripleAMuse"

[build.environment]
  NODE_VERSION = "20"
  VITE_NETLIFY = "true"

# Include shared functions
[functions]
  directory = "../netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["mongoose"]
  included_files = ["../server/**", "../packages/**"]

# API redirects - /api/* goes to the serverless function
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true

# SPA routing - all other requests go to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
