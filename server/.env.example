MONGO_URI=mongodb://localhost:27017/triplea
JWT_SECRET=replace-with-a-long-random-secret
PORT=4000

# Optional: seed demo musicians/instruments into Mongo on startup if collections are empty.
SEED_DEMO_DATA=true
